[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "dotnet AquaLab.dll"
healthcheckPath = "/api/rapports/dashboard"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
